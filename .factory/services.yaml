commands:
  install: cargo fetch --locked
  typecheck: cargo check --workspace
  build: cargo test -q --workspace --no-run
  test: cargo test -q --workspace
  lint: cargo fmt --all --check
  docs_audit: cargo test -q -p openai-agents --test behavior_parity --test root_export_parity

services: {}
